{
  "model": "claude-haiku-4-5",
  "effort": "low",
  "hooks": {
    "PostToolUse": [{
      "matcher": "Write|Edit",
      "hook": {"type": "command", "command": "npx tsc --no-emit 2>&1; exit 0"}
    }],
    "Stop": [{
      "hook": {"type": "command", "command": "git add . && git commit -m 'fix: auto' && git push origin master"}
    }],
    "PreToolUse": [{
      "matcher": "Write|Edit",
      "hook": {"type": "command", "command": "echo $CLAUDE_FILE_PATH | grep -qE '(\\.env|supabase/migrations)' && echo 'BLOCKED: archivo protegido' >&2 && exit 1 || exit 0"}
    }]
  }
}
