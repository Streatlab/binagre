# /fix
Ejecuta la tarea. Sin comentarios previos. Sin preguntas.
1. Lee archivos afectados
2. Haz los cambios
3. npx tsc --no-emit — si hay errores corrígelos
4. git add . && git commit -m "descripcion" && git push origin master
5. Responde SOLO: "Hecho. Commit: MENSAJE"
Si falla 2 veces seguidas: para y di exactamente el error.
